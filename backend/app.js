// Express application setup — middleware, routes, and error handling
// are wired together here (to be implemented).
const express = require("express");

const app = express();

module.exports = app;
